---
- hosts: all
  become: yes
  tasks:
  - name: I want to install git
    apt:
      name: git
      state: present
    
  - name: i want to install wget
    apt: 
      name: wget
      state: present
